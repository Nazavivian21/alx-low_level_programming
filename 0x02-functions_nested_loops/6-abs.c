#include "main.h"

/**
 * _abs - prints out an absolute number
 * Description: Does the stuff
 * @num: the variable used to check
 * Return: an absolute number
 */
int _abs(int num)
{

	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
