/**
 * add - a function that adds numbers
 * @a: an integer
 * @b: an integer that adds to b
 * Description: It adds two integer values
 * Return: the value of the integer
 */
int add(int a, int b)
{
	return (a + b);
}
