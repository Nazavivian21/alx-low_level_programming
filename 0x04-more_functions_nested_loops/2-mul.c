#include "main.h"
/**
 * mul - a function name
 * Description: adds two integers 
 * @a: first integer to be added
 * @b: second integer to be added
 * @c: stores the sum
 * Return: 0 on success
 */
int mul(int a, int b)
{
	int c;
	c = (a * b);
	return (c);
}
